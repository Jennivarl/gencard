GenFren Frontend (minimal)

Run locally (PowerShell):

```powershell
cd frontend
npm install
npm run dev
```

Open http://localhost:3000 to preview. The `/api/verify` endpoint is mocked; replace it with a GenLayer call after you deploy `contracts/verified_card.py`.
